# User Requirements

## Stage E

- Environment: create a Python 3.11 Conda environment named `lunar-lander`.
- Device: local CPU for tests and short smoke runs; Katana for long training.
- Data: no external dataset; scenarios are generated from frozen manifests.
- Execution: automatically run fast tests and smoke checks; do not run the full 60-model study locally.
- Git: initialize and push the whole project to `https://github.com/cattiesu2025/lunarlander_transition_timing.git` using the existing global identity.
- README: place at the project root.

### Document Preferences

- Language: Chinese prose with English technical identifiers where useful.
- Preserve the scientific constraints and interpretation boundaries in `lunar_lander_plan.md`.
